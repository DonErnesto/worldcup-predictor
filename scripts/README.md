# Scripts

Planned scripts:

- `parse_worldcup_results.*` - convert OpenFootball files into tabular match rows.
- `normalize_fifa_rankings.*` - flatten FIFA ranking JSON.
- `train_baseline_model.*` - train and save the first model artifact.
