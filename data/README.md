# Data

Raw downloads live in `raw/` and should not be edited manually.

Processed datasets should be written to `processed/` by repeatable scripts. A good first target is:

- `processed/world_cup_matches.csv`
- `processed/fifa_rankings_latest.csv`
- `processed/training_examples.csv`
